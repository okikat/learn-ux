# CLAUDE.md — さわって学ぶUX 開発ガイド

UXの心理法則を「さわって学ぶ」インタラクティブ学習アプリ。
React 19 + TypeScript + Vite / CSS Modules + CSSカスタムプロパティ / react-router-dom(HashRouter) / 状態は useState / Vitest。

## このアプリの大原則（最重要）

**このアプリ自身が、UXの良い手本でなければならない。**「UXの教科書」を名乗る以上、
土台のUXが甘いと説得力を失う。細部まで妥協しない。すべての変更は次を満たすこと。
（この方針は、ユーザーの度重なる修正指示から言語化したもの。以降は指示を待たず先回りで適用する。）

### 1. レイアウトを動かさない（ガタつき厳禁）
状態変化（トグル・回答前後・展開/折りたたみ・文言の出し分け）や、**文字数・行数・段数・
桁数の違い**で、**枠・バー・ボタン・図の位置やサイズが伸び縮みしてはいけない。**
取り得る**最大の状態に合わせて固定**する。
- 推奨実装：複数の文言/ラベルを **CSS grid で同じセルに重ね**、非アクティブ側は
  `visibility: hidden`（`display:none` は不可＝場所が消える）で**場所を確保**する
  （例: `pragnanz` / `von-restorff`）。
- 図やコントロールの位置は、操作の前後で動かさない。

### 2. 数値・バーは「比較できる」形に
- 比較する対（大↔小・近↔遠・A↔B など）は**隣接**させる。
- 数値は**等幅フォント・右そろえ・固定幅の列**にし、桁数でバーが縮まない／**終点がそろう**
  ようにする（例: `hick` / `jakob` / `aesthetic` / `fitts`）。
- 冗長な表記は避ける。**時間は「0.0秒」（小数1桁）に統一**（"3.22秒(3216ms)"併記やms単独表記は使わない）。ただし固定ラベルのボタン（例: 1秒/2秒）はそのまま。

### 3. モバイル最優先のタッチ配慮
- **`:hover` は `@media (hover: hover)` でホバー可能端末のみ**に限定（タッチで触れた箇所が
  「選択中」風に残るのを防ぐ）。
- タップ/ドラッグの**当たり判定を広く**（スライダーのツマミ等。見た目は維持したまま判定だけ拡大）。
- 縦に長いテスト枠は、開始時に**枠の上端を画面内へスクロール**して全体を収める。
- 開始タップのすり抜け等による**誤操作を防ぐ**。
- 入力欄（`type=date` 等）が**枠からはみ出さない**（`appearance:none` / `min-width:0`）。
- **文字の折り返しに注意（モバイル幅で特に）。** テスト枠内・ボタン/タブ/チップ内・問い/説明文が、
  狭い画面で**不格好に折り返す／枠からはみ出す**のを防ぐ。
  - ボタン等の中の文字は**折り返さない長さに収める**（2行化で高さが変わる＝原則1のガタつきになる）。長い時は文言を短く。
  - 問い・説明文は中途半端な位置で折れないよう、**文の区切りで改行を制御**する（例: `framing` の問いを2段に）。
  - 長い語/数値/URLが溢れる時だけ `overflow-wrap` 等で対処（不自然な切れ方は避ける）。
  - 最小幅 **375px（できれば 320px）** で、枠内・ボタン内の折り返しを必ず確認する。

### 4. スマホ・PC 両対応 ＆ 一貫性
最小幅 **375px**（できれば 320px）〜広い画面まで破綻しない。タップ標的は `var(--tap)`（44px）以上。
`prefers-reduced-motion` 尊重。**同じ要素はPCとスマホで見た目・機能をそろえる**（例：日付欄の📅）。

### 5. ライト/ダーク両対応（毎回確認）
色は**必ずデザイントークン**（`src/index.css`）経由。
- 直書き色（`#fff`・`#000`・`rgba(0,0,0,..)` 等）と **`mix-blend-mode`** は禁止
  （ダークで「白×白」「色の濁り」を生む）。
- 薄すぎる線・低コントラスト文字をなくす（視認性）。変更後は**両テーマ**で確認。

### 6. デモ（例）は「一目で腑に落ちる」ことが最優先
- 違いが**体感・一目で**分かること。分かりにくければ**例ごと作り替える**。
- **誘導しない**（「◯◯を探して」と先に促すと、その法則が成立しなくなる）。
- **「悪い例 → 良い例」のコントラスト**で印象づける。
- 可能なら**説明そのものを法則の実例に**する（自己言及。例: `similarity` の青字リンク）。

### 7. 文言・表記は簡潔・一貫・正確
表記ゆれをなくし（ひらがな/漢字＝「さわって」、秒/ms）、**実際の値と説明文を一致**させ、
句読点・括弧など細部まで整える。

### 8. アクセシビリティ
意味のある画像/SVGには `role="img"` と `aria-label`。装飾は `aria-hidden`。
キーボード操作・フォーカス可視（`:focus-visible` は index.css で共通化済み）。

## コマンド
```bash
npm ci          # 依存インストール
npm run build   # tsc -b && vite build（型チェック込み）
npm test        # Vitest（純関数＋主要コンポーネント）
```
変更したら **build と test を必ず緑にする**。

## 公開（GitHub Pages / gh-pages ブランチ配信）
ビルド成果物 `dist` を `gh-pages` に配信。手順は `docs/HANDOFF.md` 参照。
反映確認は `https://okikat.github.io/learn-ux/?probe=$(date +%s)` を curl して
参照アセット名が新ビルドと一致するかで判定（Pages 反映に 1〜2 分）。

## 構成
```
src/
├── components/  共通レイアウト・カード・法則ページの型・メニュー など
├── pages/       トップ / 各法則ページ
├── laws/        各法則のインタラクティブ・デモ（1法則=1フォルダ）
├── data/laws.tsx 法則のメタデータ（定義・解説・Tips・カテゴリ）。description は ReactNode 可
└── utils/       計測まわりの純関数（テスト付き）＋ useTheme
```

## ブランチ
開発は作業ブランチ（例: `claude/busy-meitner-1Tgkh`）。`main` への直 push はしない。
